# Development Dockerfile for JSON Toolkit
# This creates a development environment with hot reload capabilities

FROM node:22-alpine

# Set working directory
WORKDIR /app

# Add package files for dependency installation
COPY package*.json ./

# Install dependencies (including dev dependencies)
RUN npm install --legacy-peer-deps

# Copy source code
COPY . .

# Expose the development server port
EXPOSE 5173

# Create non-root user for security
RUN addgroup -g 1001 -S nodejs && \
    adduser -S nextjs -u 1001

# Change ownership of the app directory
RUN chown -R nextjs:nodejs /app
USER nextjs

# Start the development server with host binding for Docker
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]